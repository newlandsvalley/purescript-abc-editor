{
  "private": true,
  "scripts": {
    "build": "pulp build",
    "example": "pulp build --no-check-main --main Example.Main --include example/src --to example/dist/abc-editor.js",
    "watch": "pulp -w build --to dist/abc-editor.js"
  },
  "devDependencies": {
    "pulp": "^13.0.0",
    "purescript": "^0.13.3",
    "purescript-psa": "^0.5.1",
    "webpack": "^3.6.0"
  }
}
