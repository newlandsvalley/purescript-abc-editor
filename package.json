{
  "private": true,
  "scripts": {
    "build": "pulp build",
    "example": "pulp build --no-check-main --main Example.Main --include example/src --to example/midi-melody/abc-editor-midi.js"
  },
  "devDependencies": {
    "pulp": "^14.0.0",
    "purescript": "^0.13.8",
    "purescript-psa": "^0.5.1",
    "webpack": "^3.6.0"
  }
}
