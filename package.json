{
  "private": true,
  "scripts": {
    "build": "pulp build --to dist/abc-editor.js",
    "minimize": "uglifyjs dist/abc-editor.js --output dist/min.js --compress",
    "watch": "pulp -w build --to dist/abc-editor.js"
  },
  "devDependencies": {
    "pulp": "^13.0.0",
    "purescript": "^0.13.3",
    "purescript-psa": "^0.5.1",
    "webpack": "^3.6.0"
  }
}
